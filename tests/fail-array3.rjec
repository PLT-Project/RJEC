/*  Failure test: cannot define assign value with a different type than the 
    defined array type to an array item
 */

struct foo {
    x int;
    y char;
    z bool;
}

func main () {
    var a [10]struct foo;
    for i := 0; i < 10; i = i + 1 {
        a[i] = i;
    }
}