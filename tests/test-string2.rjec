func main () {
    x := "ello";
    for i := 0; i < 4; i = i + 1 {
        printc(x[i]);
    }
    prints(x);

    x[1] = x[2];
    x[3] = x[0];
    for i := 0; i < 4; i = i + 1 {
        printc(x[i]);
    }
    prints(x);
}