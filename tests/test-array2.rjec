/* Array test: declare an array of structs */

struct foo {
    x int;
    y char;
    z bool;
}

func main () {
    var a [10]struct foo;
    for i := 0; i < 10; i = i + 1 {
        s := a[i];
        printi(s.x);
        printb(s.z);
    }
}